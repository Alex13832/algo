dist: trusty
sudo: required
language: cpp
compiler:
  - gcc
os: osx

before_install:
  - brew install openssl
  - eval "$(pyenv init -)"
  - pyenv rehash
  - pip install cpp-coveralls
  - pyenv rehash

script:
  - ./configure --enable-gcov
  - cmake .
  - cmake --build .
  - make
  - cd test
  - ./algo_tests

after_success:
  - coveralls